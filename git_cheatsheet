// Initialize git which creates .git folder
git init

// Adding all changes in working directory to the stage area
git add .

// Adding multiple changes from working directory to the staging area (first-file, second-file)
git add <first-file> <second-file>

// Commiting a change with message. 1: Add the changes to staging area 2: Commit with message
1: git add.
2: git commit -m "your-message-here"

// Amend the last commit. 1: Stage your forgotten changes 2: Amend your last commit 3: Write your commit on terminal and hit esc, then wq!
1: git add <my-forgotten-changes>
2: git commit --amend

// Change username
git config user.name "YOURUSERNAME"

// Show username
git config user.name

// Set to update the local list of remote git branches automatically every time you run git pull or git fetch using below command.
git config remote.origin.prune true

// Update local list of remote branches
git remote update origin --prune

// Show current branch
git branch

// Create a new branch from current branch
git branch <branch-name>

// Create a new branch from current branch and switch to that branch (2 ways)
git switch -c <new-branch-name-to-create>
git checkout -b <new-branch-name-to-create>

// Switch branch (2 ways)
git switch <branch-name-to-switch>
git checkout <branch-name-to-switch>

// Show the commit logs
git log

// Show the commit logs in oneline
git log --oneline
